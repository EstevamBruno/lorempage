import { HoverPhotoDirective } from './hover-photo.directive';

describe('HoverPhotoDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverPhotoDirective();
    expect(directive).toBeTruthy();
  });
});
